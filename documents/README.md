Place local annual reports, 8-Ks, and 10-Ks in this directory before running the document ingestion workflow.

Expected inputs:
- PDF files only
- One file per document
- Filenames should be stable because the deployment script uses them as document identifiers

This directory is intentionally kept out of git for PDF binaries.